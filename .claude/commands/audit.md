Your goal is to update any vulnerable dependencies.

Do the following:

1. Run the `npm audit` to find vulnerable installed packages in this project
2. Run the `npm audit fix` to apply updates
3. Run tests and verufy the updates didn't break anythng


